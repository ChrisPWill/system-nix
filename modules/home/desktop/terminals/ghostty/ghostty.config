# Dynamic configuration for ghostty.
# Changes here are reflected immediately without a nix rebuild.

# Screen bloom
# custom-shader = shaders/bloom.glsl

# Cursor effects (Managed by Nix in default.nix, but be added to here)
# custom-shader = shaders/cursor_warp.glsl
# custom-shader = shaders/rectangle_boom_cursor.glsl

