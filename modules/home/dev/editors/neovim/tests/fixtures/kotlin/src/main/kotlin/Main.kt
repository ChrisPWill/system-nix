package fixture

fun fixtureTargetAlpha(value: String): String {
    return value.uppercase()
}

fun main() {
        fixtureTargetAl
        if (true) {
                val styleIssue= "EditorConfig should control formatting"
                println(styleIssue)
        }
}
